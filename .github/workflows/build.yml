name: Build Android APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Install Dependencies
        run: npm install --force

      - name: Build Web Project
        run: |
          npm run build -- --base=./ || npm run build || true
          if [ ! -d "dist" ]; then
            mkdir -p dist
            cp index.html dist/ 2>/dev/null || true
          fi

      - name: Setup Capacitor Config
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android
          
          cat <<EOF > capacitor.config.json
          {
            "appId": "com.ec.aoo",
            "appName": "PolyGuide",
            "webDir": "dist",
            "server": {
              "url": "https://polyguide.vercel.app/login",
              "androidScheme": "https",
              "cleartext": true
            }
          }
          EOF
          
          npx cap add android || true
          npx cap sync android

      - name: Setup App Icon (If available)
        run: |
          # Copy uploaded logo file to standard asset paths
          if [ -f "Max_a_ebar_name_sara_sudu_.png" ]; then
            cp "Max_a_ebar_name_sara_sudu_.png" "public/hero.png"
            cp "Max_a_ebar_name_sara_sudu_.png" "public/icon.png"
            cp "Max_a_ebar_name_sara_sudu_.png" "public/logo.png"
            cp "Max_a_ebar_name_sara_sudu_.png" "src/assets/hero.png"
            cp "Max_a_ebar_name_sara_sudu_.png" "src/assets/icon.png"
            cp "Max_a_ebar_name_sara_sudu_.png" "src/assets/logo.png"
          fi

          if [ -f "public/hero.png" ] || [ -f "Max_a_ebar_name_sara_sudu_.png" ]; then
            mkdir -p assets
            SOURCE_IMG="public/hero.png"
            if [ -f "Max_a_ebar_name_sara_sudu_.png" ]; then
              SOURCE_IMG="Max_a_ebar_name_sara_sudu_.png"
            fi
            cp "$SOURCE_IMG" assets/icon.png
            cp "$SOURCE_IMG" assets/icon-only.png
            cp "$SOURCE_IMG" assets/icon-foreground.png
            cp "$SOURCE_IMG" assets/splash.png
            cp "$SOURCE_IMG" assets/splash-dark.png

            npm install --save-dev @capacitor/assets
            npx --yes @capacitor/assets generate --android || true
          fi

      - name: Setup Firebase Google Services
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          if [ -n "$GOOGLE_SERVICES_JSON" ]; then
            echo "$GOOGLE_SERVICES_JSON" > android/app/google-services.json
            echo "Google services JSON created successfully."
          else
            echo "Warning: GOOGLE_SERVICES_JSON secret is empty or missing. Build may fail if Push is enabled."
            # Remove plugin if missing to avoid failure
            sed -i 's/apply plugin: "com.google.gms.google-services"//g' android/app/build.gradle || true
          fi

      - name: Build Android APK
        run: |
          cd android
          
          echo "configurations.all {
              resolutionStrategy.eachDependency { DependencyResolveDetails details ->
                  if (details.requested.group == 'org.jetbrains.kotlin') {
                      details.useVersion '1.8.22'
                  }
              }
          }" >> app/build.gradle
          
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Mobile-App-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
